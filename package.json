{
  "name": "random-framework",
  "version": "0.1.0",
  "description": "RanDOM - A paradigm-shifting web framework where AI dynamically determines DOM responses",
  "private": true,
  "scripts": {
    "start": "python -m backend.main",
    "dev": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "keywords": [
    "ai",
    "framework",
    "dom",
    "virtual-dom",
    "snabbdom",
    "openai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "snabbdom": "^3.5.1"
  }
}

